@model IEnumerable<SchoolManagementSystem.Models.TeacherExamRoutine>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Exam)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Teacher)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Exam.ExamId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Teacher.TeacherId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TeacherExamRoutineId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TeacherExamRoutineId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TeacherExamRoutineId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
